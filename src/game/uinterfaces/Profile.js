import RuntimeUI from "./RuntimeUI";

class Profile extends RuntimeUI {
    constructor (scene) {
        super(scene, "profile"); 
    }

    append () {
        super.append();
    }
};

export default Profile;